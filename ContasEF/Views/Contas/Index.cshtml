
@model ContasEF.Models.ContaLuz

@{
    ViewData["Title"] = "Contas";
}
<h2 class="text-center">Contas de Luz</h2>
<p class="d-block text-right">
    @Html.ActionLink("Adicionar Conta de Luz", "Create", new {}, new { @class = "btn btn-outline-info" })
</p>
<table class="table table-bordered table-striped">
    <thead class="thead-dark">
        <tr>
            <th>ID</th>
            <th>Data de leitura</th>
            <th>KW Gasto</th>
            <th>Valor a pagar</th>
            <th>Data de pagamento</th>
            <th>Média de consumo</th>
            
        </tr>
    </thead>

    <tbody>
        @foreach (var item in Model.listaContasLuz)
        {
            <tr>
                <th>@item.id</th>
                <td>@item.dataLeitura.ToString("dd/MM/yyyy")</td>
                <td>@item.kwGasto</td>
                <td>R$ @item.valorConta</td>
                <td>@item.dataPagamento.ToString("dd/MM/yyyy")</td>
                <td>@item.consumoMedio</td>
                <td class="text-center">
                    <a class="btn btn-outline-success" 
                        asp-controller="ContaLuz" 
                        asp-action="View" 
                        asp-route-id="@item.id"><i class="fas fa-eye"></i></a>

                    <a class="btn btn-outline-primary mx-2" 
                        asp-controller="ContaLuz" 
                        asp-action="Update" 
                        asp-route-id="@item.id"><i class="fas fa-edit"></i></a>

                    <a class="btn btn-outline-danger" 
                        asp-controller="ContaLuz" 
                        asp-action="Delete" 
                        asp-route-id="@item.id"><i class="fas fa-trash-alt"></i></a>
                </td>
            </tr>
        }
        @if (!Model.listaContasLuz.Any())
        {
            <td colspan="7" class="text-center"> Nenhum registro cadastrado </td>
        }
    </tbody>
</table>

@if (Model.listaContasLuz.Any())
{
    <div class="row">
        <div class="col-sm-6">
            <div class="card">
                <h5 class="card-header">Menor consumo</h5>
                <div class="card-body">
                    <div class="form-group col-12">
                        <div class="editor-label">
                            Data da eitura
                        </div>
                        <div class="editor-field">
                            <input type="text" value="@Model.menor.dataLeitura.ToString("dd/MM/yyyy")" class="form-control" disabled/>
                        </div>
                    </div>

                    <div class="form-group col-12">
                        <div class="editor-label">
                            KW Gasto
                        </div>
                        <div class="editor-field">
                            <input type="text" value="@Model.menor.kwGasto" class="form-control" disabled/>
                        </div>
                    </div>

                    <div class="form-group col-12">
                        <div class="editor-label">
                            Valor a pagar
                        </div>
                        <div class="editor-field">
                            <input type="text" value="R$ @Model.menor.valorConta" class="form-control" disabled/>
                        </div>
                    </div>

                    <div class="form-group col-12">
                        <div class="editor-label">
                            Data de pagamento
                        </div>
                        <div class="editor-field">
                            <input type="text" value="@Model.menor.dataPagamento.ToString("dd/MM/yyyy")" class="form-control" disabled/>
                        </div>
                    </div>

                    <div class="form-group col-12">
                        <div class="editor-label">
                            Consumo medio
                        </div>
                        <div class="editor-field">
                            <input type="text" value="@Model.menor.consumoMedio" class="form-control" disabled/>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        <div class="col-sm-6">
            <div class="card">
                <h5 class="card-header">Maior consumo de energia</h5>
                <div class="card-body">
                    <div class="form-group col-12">
                        <div class="editor-label">
                            Data de leitura
                        </div>
                        <div class="editor-field">
                            <input type="text" value="@Model.maior.dataLeitura.ToString("dd/MM/yyyy")" class="form-control" disabled/>
                        </div>
                    </div>

                    <div class="form-group col-12">
                        <div class="editor-label">
                            KW Gasto
                        </div>
                        <div class="editor-field">
                            <input type="text" value="@Model.maior.kwGasto" class="form-control" disabled/>
                        </div>
                    </div>

                    <div class="form-group col-12">
                        <div class="editor-label">
                            Valor a pagar
                        </div>
                        <div class="editor-field">
                            <input type="text" value="R$ @Model.maior.valorConta" class="form-control" disabled/>
                        </div>
                    </div>

                    <div class="form-group col-12">
                        <div class="editor-label">
                            Data de pagamento
                        </div>
                        <div class="editor-field">
                            <input type="text" value="@Model.maior.dataPagamento.ToString("dd/MM/yyyy")" class="form-control" disabled/>
                        </div>
                    </div>

                    <div class="form-group col-12">
                        <div class="editor-label">
                            Consumo mdeio
                        </div>
                        <div class="editor-field">
                            <input type="text" value="@Model.maior.consumoMedio" class="form-control" disabled/>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
}